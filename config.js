/* eslint-env node */
// /** @type {{[key: string]: any}} */
// let local = {};

// if (process.env.NODE_ENV !== 'production') {
//   try {
//     ({ default: local } = require('./config.local.js'));
//   } catch (error) {
//     console.error('no local config file', { error });
//   }
// }

const config = {
  baseUrl: 'https://cloudrunservice-rd2h3qv36q-ew.a.run.app/',
};

export default config;
